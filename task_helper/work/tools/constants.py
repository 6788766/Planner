"""
Small constants needed by the WorkBench tools.
"""

# Keep deterministic so state-change evaluation is stable.
# Stored as a string because the underlying CSVs use string timestamps.
HARDCODED_CURRENT_TIME = "2023-11-30 12:00:00"

